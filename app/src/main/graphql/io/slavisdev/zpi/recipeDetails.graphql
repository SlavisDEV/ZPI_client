query RecipeDetails($id: Int) {
    recipe(id: $id) {
        title,
        description,
        preparationTime,
        servings,
        imageSet {
            url
        },
        ingredients {
            name
        }
    }
}